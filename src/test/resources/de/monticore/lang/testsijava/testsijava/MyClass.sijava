package de.monticore.testsijava;

siclass MyClass {
    double in km/h var = 3.0 m/s;
    int varInt = 3;
    metod int in m method1 (int varInt2) {
       int in km/ms^2 var2 = 4 m/s^2;
       var2 = 5 m / (2s * 5ms);
       var2 = 5 km/ks^2;
       var2 *= varInt2;
       int in h varS = 3s;
       int in dm varI_M = 10 m;
       int in dm varI_M2 = 10 * method2();
    }

    metod m method2 () {
        si varM = m;
        si varS = s;
        si varSomething = varM * varS;
        int in m*s varI_MS = varSomething * 3;
    }
}